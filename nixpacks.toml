[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

[variables]
NODE_VERSION = "20"
```

Salva, e no terminal:
```
git add nixpacks.toml
git commit -m "fix: força Node.js 20"
git push